<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DynamicFeatureApp"
        tools:targetApi="31">

        <activity
            android:name="com.testarchitecture.dashboard.DashboardActivity"
            android:icon="@mipmap/ic_launcher_new"
            android:label="@string/app_name_new"
            android:roundIcon="@mipmap/ic_launcher_new_round"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="LegacyDashboardActivity"
            android:icon="@mipmap/ic_launcher_legacy"
            android:label="@string/app_name_legacy"
            android:roundIcon="@mipmap/ic_launcher_legacy_round"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>